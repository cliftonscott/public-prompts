# Prompt files

Add `.md` files in **`general/`**.

Suggested header for each prompt:

```markdown
# Title

**Tool:** General  
**Use when:** One sentence describing when to use this prompt.

Trigger phrases: "example phrase", "/slash-command", etc.

---
```

Then the instructions body.
